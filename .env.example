# MySQL-compatible database reachable from Vercel (PlanetScale, Aiven, Railway, Neon via compatible endpoint, etc.)
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/apt_warehouse?sslaccept=strict"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-secret"
